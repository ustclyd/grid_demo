class_name TurnPresentation
extends RefCounted


func build_events(turn_result: TurnResult) -> Array[PlaybackEvent]:
	var events: Array[PlaybackEvent] = []

	# Stub only.
	# Future implementation should translate TurnResult into ordered playback
	# events that follow the presentation timeline document.

	return events
